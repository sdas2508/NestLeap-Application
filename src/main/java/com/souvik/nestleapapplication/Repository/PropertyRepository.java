package com.souvik.nestleapapplication.Repository;

// ✅ Placeholder repository — exists for consistency, not for DB use
// In the future, if you store PropertyDetails in a database, 
// you can just extend JpaRepository here.

public interface PropertyRepository {
    // Currently unused because PropertyDetails comes from data.json
}
